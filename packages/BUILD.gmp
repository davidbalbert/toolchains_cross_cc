load("@rules_foreign_cc//foreign_cc:defs.bzl", "configure_make")

filegroup(
    name = "srcs",
    srcs = glob(["**"]),
)

configure_make(
    name = "libgmp",
    lib_source = ":srcs",
    out_data_dirs = ["share"],
    visibility = ["//visibility:public"],
)
