load("@rules_foreign_cc//foreign_cc:defs.bzl", "configure_make")

filegroup(
    name = "srcs",
    srcs = glob(["**"]),
)

configure_make(
    name = "libmpfr",
    lib_source = ":srcs",
    deps = ["@gmp//:libgmp"],
    out_data_dirs = ["share"],
    visibility = ["//visibility:public"],
)
